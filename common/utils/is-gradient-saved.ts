// const isGradientSaved = (id: string): boolean => {
//   const favGradientsStr = localStorage.getItem("gradients");
//   if (favGradientsStr) {
//     return JSON.parse(favGradientsStr).includes(id);
//   } else return false;
// };

// export default isGradientSaved;
