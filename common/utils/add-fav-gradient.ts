// const setFavGradient = (id: string) => {
//   const favGradientsStr = localStorage.getItem("gradients");

//   if (favGradientsStr) {
//     const favGradients: string[] = JSON.parse(favGradientsStr);

//     if (favGradients.includes(id))
//       localStorage.setItem("gradients", JSON.stringify(favGradients.push(id)));
//   } else {
//     localStorage.setItem("gradients", JSON.stringify([id]));
//   }
// };

// export default setFavGradient;
